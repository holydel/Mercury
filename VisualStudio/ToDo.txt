Save ImGUI context even if swapchain recreating (init engine - init imgui context - init window/swapchain - init imgui rendering backend (try to save font textures))

Pass swapchain parameters

D3D12 swapchain resize
